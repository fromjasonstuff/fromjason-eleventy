               <aside class="masthead">
                  <img src="{{ settings.siteRoot}}/img/{{ logo }}" loading="lazy" alt="grotesque mask illustration" class="deckhead-img">
                  <h1 class="display">{{ title }}</h1>
                  <p style="margin-bottom: 5px;" class="lead enter-text">{{ description }}</p>
                  {% include "partials/cta.njk" %}
               </aside>
